# general settings
name: FDAN_train_Vimeo_BI
model_type: VideoRecurrentModel
scale: 4
num_gpu: auto  # official: 2 GPUs
manual_seed: 0

# dataset and data loader settings
datasets:
  train:
    name: Vimeo90K
    type: Vimeo90KRecurrentDataset
    dataroot_gt: /share3/home/zqiang/Vimeo90K/sequences
    dataroot_lq: /share3/home/zqiang/Vimeo90K/sequences_BI
    meta_info_file: /share3/home/zqiang/TMP/basicsr/data/meta_info/meta_info_Vimeo90K_train_GT.txt
    io_backend:
      type: disk

    num_frame: 15    ######
    gt_size: 256
    interval_list: [1]
    random_reverse: false
    use_hflip: true
    use_rot: true
    flip_sequence: true

    # data loader
    num_worker_per_gpu: 6
    batch_size_per_gpu: 16
    # batch_size_per_gpu: 1 # only for testing code
    dataset_enlarge_ratio: 200
    prefetch_mode: ~

  val:
    name: Vid4
    type: VideoRecurrentTestDataset
    dataroot_gt: /share3/home/zqiang/Vimeo90K/Vid4/GT
    dataroot_lq: /share3/home/zqiang/Vimeo90K/Vid4/BIx4

    cache_data: True
    io_backend:
      type: disk

    num_frame: -1  # not needed


# network structures
network_g:
  type: FDAN
  num_feat: 64
  num_extract_block: 0
  num_block: 30
  num_levels: 1
  num_heads: 8
  num_points: 4
  max_residue_magnitude: 10
  flownet_path: experiments/pretrained_models/flownet/fastflownet_ft_sintel.pth
  one_stage_up: true

# path
path:
  pretrain_network_g: /share3/home/zqiang/TMP/experiments/FDAN_train_Vimeo_BI_11/models/net_g_30000.pth
  strict_load_g: true
  resume_state: ~

# training settings
train:
  ema_decay: 0.999
  optim_g:
    type: Adam
    lr: !!float 2e-4
    weight_decay: 0
    betas: [0.9, 0.99]

  scheduler:
    type: CosineAnnealingRestartLR
    periods: [300000, 300000]
    restart_weights: [1, 1]
    eta_min: !!float 1e-7

  total_iter: 600000
  warmup_iter: -1  # no warm up

  # losses
  pixel_opt:
    type: CharbonnierLoss
    loss_weight: 1.0
    reduction: mean

# validation settings
val:
  val_freq: !!float 1e4
  save_img: false

  metrics:
    psnr: # metric name, can be arbitrary
      type: calculate_psnr
      crop_border: 0
      test_y_channel: true

# logging settings
logger:
  print_freq: 1000
  save_checkpoint_freq: !!float 1e3
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500

find_unused_parameters: true
